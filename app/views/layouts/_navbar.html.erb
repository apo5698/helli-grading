<nav class="navbar navbar-expand navbar-dark bg-dark fixed-top shadow">
  <button class="btn">
    <svg width="1.5em" height="1.5em" viewBox="0 0 16 16" class="bi bi-list white" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
      <path fill-rule="evenodd" d="M2.5 11.5A.5.5 0 0 1 3 11h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4A.5.5 0 0 1 3 7h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4A.5.5 0 0 1 3 3h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5z"/>
    </svg>
  </button>

  <%= link_to('Helli Dev', '', { class: 'navbar-brand' }) %>

  <div class="mr-auto"></div>

  <ul class="navbar-nav">
    <li class="nav-item dropdown show">
      <a class="nav-link" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
        <i class="fas fa-user-circle fa-2x"></i>
      </a>
      <div class="dropdown-menu dropdown-menu-right">
        <%= link_to('Profile', '#', { class: 'dropdown-item' }) %>
        <div class="dropdown-divider"></div>
        <% session_path = session_path(session[:user_id]) %>
        <%= link_to('Logout', session_path, { class: 'dropdown-item', method: :delete }) %>
      </div>
    </li>
  </ul>
</nav>

<% content_for :js do %>
  <script>
    $('a[href="<%=session_path %>"]').click(() => {
      localStorage.clear();
    });
  </script>
<% end %>
