<%= render partial: '/rubric_items/item', locals: {usage: 'This step runs the teaching-staff\'s white box tests against the selected input file.',
                                                   required_items: %w(input_file test_file type points criterion response),
                                                   rubric_item: rubric_item,
                                                   default_set: RubricItem.default_set(:ts_wbt),
                                                   default_description: RubricItem.default_description(:ts_wbt)} %>
