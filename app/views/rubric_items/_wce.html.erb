<%= render partial: '/rubric_items/item', locals: {usage: 'This step compiles and executes the selected input file.',
                                                   required_items: %w(input_file type points criterion response),
                                                   rubric_item: rubric_item,
                                                   default_set: RubricItem.default_set(:wce),
                                                   default_description: RubricItem.default_description(:wce)} %>
